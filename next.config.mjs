/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

module.exports = {
    images: {
      domains: ['https://main.d1cjex1r9btrps.amplifyapp.com/'],
    },
  };